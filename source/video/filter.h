/*
 * Snepulator video filtering.
 */


/* Dim the non-active part of the video_out_texture data. */
void video_filter_dim_border (void);

/* Dot Matrix effect. */
void video_filter_dot_matrix (void);

/* Scanline effect. */
void video_filter_scanlines (void);
